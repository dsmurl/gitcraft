# Default dev database (SQLite)
# The file path is resolved relative to schema.prisma (libs/prisma/prisma/schema.prisma)
# Store the SQLite file inside the prisma library for a self-contained setup.
DATABASE_URL="file:./data/dev.db"

# For Postgres, use:
# DATABASE_URL="postgresql://user:password@localhost:5432/gitcraft?schema=public"
